extends ./layout
block content
  h1 Administration

  -if(data.matches.length == 0){
      = 'Aucun événement en cours'
      br
  -}else{
    each match in data.matches
      .match: a(href='/admin/match/#{match.id}/show')= match.name
  -}
  
  br
  a(href='admin/match/new') Créer un nouvel évènement
  br
  a(href='admin/logout') Déconnexion